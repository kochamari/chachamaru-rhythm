# 実装進捗

CP0: 仕様を分割し、空のリポジトリから実装開始。私用音源は _private に隔離。公開・外部送信は行わない。

CP1: TypeScript strictの独立エンジン、時計・入力・ZIP検証・IndexedDB・バックアップを実装。92 unitテストが成功。
CP2: 自作64秒「ひまわり囃子」、3難易度、選曲→演奏→結果→リトライ、4打面、MIDI Learn、診断を接続。
CP3: ローカルFastAPI、最終音源からのlibrosa解析、編集、500ms自動保存、履歴、ZIP書出しを実装。Python24テスト成功。指定曲の私用パックを作成。
CP4: 生成夕景＋独立SVGリグ。Home/Library/実演奏のスクリーンショットを開き、レーンと入力領域の視認性を確認。
CP5: Chromium/WebKitを検証。数値入力のフォーカス再入、WebKitのBlob保存停止、表示の縦横比、Service Workerのscopeを修正。保存失敗や音声時計異常からの回復も確認。
CP6: Unit98件、Python26件、E2E28件が全件成功（skip0/flaky0）。npm run verify 成功。指定曲212.416秒を本番画面で全曲AUTO演奏し、100万点を確認。元音源不変・私用ZIPのSHAも再照合。
CP7: Home、Library、Playing、サビ100combo、MIDI、縦画面、全良、Studioと複数アニメーション時刻を保存して目視。reports/acceptance.json と DELIVERY.md に証拠を記録。ローカル初版完成。DEVICE=NOT_RUN、DEPLOYMENT=PENDING。

CP8: ユーザーがGitHub管理とPages公開を依頼。公開先を kochamari/chachamaru-rhythm とし、main更新時の検証・自動配信を準備。ゲーム本体と自作デモ曲が対象。私用音源・ZIP・参考資料・ローカル診断記録はGitと配信物から除外。

CP9: GitHub Pages公開成功。https://kochamari.github.io/chachamaru-rhythm/ はログイン不要で応答200。Actions run 35947393755 でUbuntu/Chromium 14件・Apple Silicon macOS/WebKit 14件、型・lint・Unit98件・Python26件・公開素材検査が成功。公開URLの新規Chromeでデモ64秒を停止なし・166良・100万点で完走。通常Chromeの時計警告後も再開して完走。私用ZIPの取込・再読み込み保存・演奏開始、440×956表示を確認。DEVICE=NOT_RUN、DEPLOYMENT=PASS。

CP10: ユーザーが4候補から選んだ手描きアニメ調にちゃちゃまるを更新。組み込みimage_genで同じ顔・色の透過パーツを作り、ホーム・選曲・演奏・仲間・結果へ統一して反映。頭・耳・前後脚・尾・スカーフの独立動作と瞬き、ドン／カッ別のバチ振りを維持。スマホ縦画面の余白も調整。npm run verifyとChromium/WebKitの全28件が成功（skip 0、flaky 0）。画像・生成プロンプト・公開許可とSHAを記録。GitHub Actions run 35951712507でも両ブラウザの検証・素材検査・Pages配信が成功。公開画像のSHA-256がローカルの採用素材と一致。通常Chromeの公開ホーム・選曲画面でも新しい絵柄を目視し、既存の私用パックが残っていることを確認。


CP11: 腕の位置と逆手の指摘を修正。承認済みの顔の画像はそのまま、肩・手首を元画像の接合点で登録し、順手の手首／バチパーツを追加。胴と腕を同一階層で動かし、CSS変換の競合を除去。太鼓は接地を維持。待機・左右打撃・ジャンプ・サビ・結果・仲間を目視し、Chromium/WebKitそれぞれ3幅×526サンプルで関節の接続とバチの面への到達を検査。npm run verifyと全30 E2Eが成功（Unit98、Python26、skip0、flaky0）。README・AGENTSから読める他AI向け docs/AI_HANDOFF.md を追加。GitHub Actions run 35955687869で両ブラウザ・ビルド・Pages配信が成功。公開ホームの順手と新関節を目視し、新素材のSHA一致・既存の保存曲の保持を確認。先行run 35955076260ではWebKitの既存Studio試聴1件が失敗したが、コードとテストを変更せず再実行して全件成功。原因未確定として引き継ぎガイドに記録。

CP12（2026-09-24・Claude）: ユーザーの「最高のアプリに。大改変してよい」という依頼と本家プレイ動画を受けて全面改修。演奏画面をPixi 1枚に統合し、本家の読みやすさに沿って「祭りの帯＋左の太鼓とコンボ＋レーン上のお祭りゲージ＋レーン下の口唱歌（ドン／カッ／ド・コ）＋ステージ」へ再構成。叩いた音符がゲージへ飛ぶ演出、良／可／不可、連打の吹き出し、サビの炎・夕暮れ空・提灯・花火、全良／フルコンボ演出、ゲージ30/50/70/100%で加わる毛色違いの仲間（黒柴・白柴・胡麻柴・赤柴）を追加。ちゃちゃまるは打面の左右で前脚を選び、ドン＝面の中央、カッ＝手前のふちを叩く。リグは1か所の定義からSVG（メニュー）とWebGL（演奏）を生成。タッチ打面は太鼓の面とふちの見た目に。打音・効果音を合成サンプル化（和太鼓／ポップ／木の音）、AudioContextを共有しiOS消音スイッチ対策。時計のゆれを平滑化し、単発の不正タイムスタンプでは一時停止しない。オリジナル曲を作り直し＋3曲追加（計4曲）：譜面を先に書き、合奏の太鼓がふつう譜面を演奏するため全音符に音が対応。タイトル・選曲（試聴・★目安・王冠・太鼓操作）・結果（数え上げ・自己ベスト）を作り直し。練習の途中開始で開始前の音符を不可にしていた不具合、ローカルStudio同期の失敗で起動全体が止まる不具合を修正。Studioの自動下書きをオンセット解析へ刷新（拍の約30ms遅れを補正して残差約3ms、半分テンポ・スウィング検出、同じ響きの小節は同じリズム）。PWAマニフェストとアイコン、Service Worker更新の案内、音符の速さ設定を追加。Unit 131件、Python 34件、E2E（Chromium/WebKit）30件が全件成功（skip 0）。実機iPhone・TD-17は未実施（DEVICE=NOT_RUN）。ユーザーの選択によりmainへ反映し公開（CP13）。

CP13（2026-09-24・Claude）: 全面改修を公開。最初のrun 35970605624はUbuntuのChromium（GPUなし＝SwiftShader）でE2E 2件が時間切れになり配信されなかった（曲は実時間で完走。READY画面の撮影11秒・クリック14秒が原因）。演奏画面のマスク4つを廃止（77→34ms/フレーム）、READY／一時停止の背景ぼかしを全環境で廃止（65→17ms）、CPU描画を判定して装飾フィルターを止め演奏を0.75倍解像度で描く（`app/gpu.ts`）、自動画質調整がプレイヤーの設定を書き換えていた不具合を修正。WebKitで再読み込み時に裏の同梱曲導入・Studio同期の読み込みが打ち切られてエラーになる競合を、`beforeunload`／`pagehide` での取り消しとBlob読み戻しの廃止で解消（再読み込み40回×2エンジンでエラー0）。E04の同一URL再読み込みを解消。Unit 133件・Python 34件・E2E 30件（Chromium/WebKit、skip 0）、SwiftShader強制のChromium 15件も成功。run 35976493358で両ブラウザの検証・build・Pages配信が成功し、公開URLを新規のChromium・WebKitで確認（新ビルド・同梱4曲・AUTO演奏・hash route）。受け入れ73 PASS／12 NOT_RUN（実機）。DEVICE=NOT_RUN、DEPLOYMENT=PASS。
CP14（2026-09-24・Claude）: 記録用のpush（run 35978634265）でCIのmacOS WebKitのE01が時間切れ（配信はスキップされ公開中の版は不変）。一時ブランチ（配信なし）で計測し、タイトルのキャラクターの `filter: drop-shadow` が毎フレーム描き直されてページ全体を止めていたと特定（rAF 92ms、IndexedDB書込み1.5MBが1540ms→影なしで17ms・10ms、同梱曲の導入23.1秒→3.4秒）。影を静的な楕円に置き換え。同runのWebKit Studio試聴（M01）の失敗はCP11と同じ現象で原因未確定。run 35989564317（commit 2926b9d）で両ブラウザ全30試験・build・Pages配信が成功（CIのWebKit全体 5.8〜7.6分→2.8分、P04 30.3秒→2.7秒、E01 1.5分）。公開版の新CSSと両エンジンでの表示・同梱4曲・hash routeを確認。ローカルでは最終E2Eの途中でこのMacの音声出力が止まり（AudioContextの時計が進まない、coreaudiod稼働9日超）、音声を使う試験が進まなかったため、この commit のE2Eの証拠はCIの結果（reports/e2e.json に統合、出典を明記）。受け入れ73 PASS／12 NOT_RUN（実機）。

CP15（2026-09-25・Claude）: ユーザーの要望で、譜面工房（Mac）へ音源をドラッグ＆ドロップするだけで曲パックZIPができるようにした。解析→3難易度→ZIP保存（`曲名_ちゃちゃまる.zip`）→このMacのゲームへ追加まで続けて行い、複数ファイルは1曲ずつ順番に処理。曲名・アーティストは入力が空ならタグ→ファイル名。別の解析中は待ってから始める（ドロップ・フォーム・下書きの作り直し）。`Start Studio.command` は別フォルダ（以前の版）の譜面工房が8787で動いていれば開かずに案内する（以前は古い譜面工房を黙って開いていた）。78秒の曲はドロップからZIPまで約5秒。Python 39件、Unit 133件、E2E 32件（新しいM14を含む、Chromium/WebKit、skip 0）が成功。ユーザーの依頼で以前の譜面工房（ChatGPTフォルダ、8787）を終了し、このフォルダの譜面工房を起動（8787、ドロップ受け口を確認）。mainへpushし、run 36017252697で両ブラウザ検証・build・Pages配信が成功（deployment 6641130469）。公開版の譜面工房は想定どおりZIP編集モード（解析はMacのみ）、同梱4曲、両エンジンでエラーなし。

CP16（2026-09-25・Claude）: ユーザーが公開サイトの譜面工房（ZIP編集モード）を開いて「MP3をドロップできない」と報告。Macの譜面工房（http://127.0.0.1:8787/#/studio）はユーザーのChromeで正常（ドロップ受け口あり、古いService Workerの影響なし）と確認。公開サイト側で「音源から作るのはMacの譜面工房」と案内し、Macでは開くボタンを出し、音源のドロップにも同じ案内を出すようにした。E2E M15を追加、M14は保存ZIPの中身（曲名・packId）で確認するよう強化。Unit 133、Python 39、E2E 34件（Chromium/WebKit）成功。

CP17（2026-09-25・Claude）: ユーザーの要望「アプリから譜面工房へ飛べるように」に対応。`~/Applications/ちゃちゃまる譜面工房.app`（`npm run mac-app` で作成、ゲームのアイコン、URLスキーム `chachamaru-studio://`）を作り、ゲームの「Macの譜面工房を開く」ボタンからも起動できるようにした。譜面工房が動いていなければ裏で起動（ターミナル不要）して開く。ユーザーのMacにアプリを作成し、URLからの起動を確認（起動中なら開くだけ、止まっていれば約2秒で起動して開く）。`studio.mjs --stop` を追加。Unit 133、Python 39、E2E 34件（Chromium/WebKit）成功。 同時に、CIのmacOS WebKitで3回目となった譜面工房の試聴の失敗（M01）を根本対応：試聴を `<audio>` からゲームと同じWeb Audioへ変更し、M01に再生位置が進むことの確認を追加。アプリ内ブラウザで再生・一時停止・連打・曲末を確認。 さらにCIで、試聴直後にWebKitの描画が止まる（rAFが来ない）ことが原因と判明。試聴ボタンの表示を描画に頼らず状態変化で更新し（`data-state`）、M01は再生開始を確認。64秒の実時間演奏を含むE01は、遅い実行環境向けにこのテストだけ上限を150秒に（確認内容は同じ）。 解析取り消しでworkerが直前に終わっていた場合のmacOSのPermissionError（CIのPythonテスト後片付けで発覚）も修正。Python 40件。

CP18（2026-09-25・Claude）: ユーザーの要望で、何曲かを1つのZIPにまとめてiPhoneへ送れるようにした。Macの譜面工房で何曲かまとめてドロップすると最後にまとめZIPを1つ保存し、前に作った曲もチェックボックスで選んでまとめられる。iPhoneの「曲を追加」でまとめZIP（や複数のZIP）を選ぶと、まだない曲だけ追加し、同じ曲は保存せず、作り直した版は更新して、結果を一覧表示する。iPhone幅でヘッダーのタイトルがメニューに重なっていたのも修正。ゲーム側の試験はiPhoneと同じくMacの譜面工房なしで動かす（裏の同期で曲数がずれる競合を防止）。Unit 135、Python 41、E2E 38件（Chromium/WebKit、skip 0）成功。

CP19（2026-09-25・Claude）: ユーザーの質問に応えて、太鼓をタップしたときの振動を追加（iPhoneはiOS 18以降のスイッチの振動を利用した軽い「コツッ」、Androidはドン14ms・カッ7ms）。設定でON/OFF、「ドンを試す」でも振動。iPhoneの幅で設定画面が右にはみ出していたのを修正し、タイミング調整の行を縦並びに。Unit 135、Python 41、E2E 40件（Chromium/WebKit）成功。実機での振動の手応えは未確認（DEVICE=NOT_RUN）。

CP20（2026-09-25・Claude）: ユーザーの5つの要望・報告に対応開始。(4) iPhoneで振動しない→iOS 26.5で塞がれた方法だったため、指が直接押す見えないスイッチを太鼓の面に重ねる方式に作り直し（振動は指を離した瞬間、実機確認はユーザー）。(5) 「音が出ず勝手に連打される」→おてほん（AUTO）への取り違えが有力と判断し、結果画面・選曲の太鼓操作でおてほんに入らないようにし、おてほん中を大きく表示。一瞬の音声停止では、まず再開を試み、戻らないときだけ一時停止。Unit 136、Python 41、E2E 40件成功。残り：(1) Bluetoothの音ズレをかんたんに合わせる、(2) 電子ドラム・横向きの画面、(3) 演出の強化。


CP21（2026-09-25・Claude）: (2) 電子ドラム・横向きの画面。横向きで電子ドラムのときは、レーン・音符（1.6倍）・判定の文字・点数・コンボを大きくした専用配置に（仲間の柴犬のステージは残す）。登録済みの電子ドラムを叩くと、メニューでも演奏中でも自動で電子ドラムに切り替わり、打面が消えて大きいレーンになる（設定にも保存）。準備画面ではスネアでも開始できる（音の許可がまだなら1回だけタップを案内）。横向きのスマホで準備・一時停止の窓が見切れていたのも修正。E2E U27を追加。Unit 136、Python 41、E2E 42件（Chromium/WebKit）成功。実機（iPhone横向き・TD-17）は未確認（DEVICE=NOT_RUN）。

CP22（2026-09-25・Claude）: (1) Bluetoothの音ズレをかんたんに。新しい「音ズレ合わせ」画面で、音を出す機器（iPhoneのスピーカー／TD-17 · Bluetoothなど）を選び、約15秒、聞こえた音に合わせてスネア（または画面・キー）を叩くと遅れを測って、その機器の「音の遅れ」に保存（音符と判定が一緒にずれて音にそろう）。小さなレーンでの試し叩きと±5/±10msの微調整も。選曲・準備画面・一時停止に「音の出力」の切り替えを置き、一時停止では±10msをその場で調整できる。設定の古い「16打」（判定だけをずらしていた）は置き換え。横向きのスマホで選曲の開始ボタンがはみ出していたのも修正。Unit 141件（新規5）、Python 41件、E2E 44件中43件成功（U28を追加）。残る1件は譜面工房の試聴（M01・WebKit、今回は未変更の箇所、CP11/CP14/CP17と同じく時々起きる）で、単独で4回再実行してすべて成功。次に起きたとき原因が分かるよう、失敗時に画面のお知らせも表示するようにした。

CP23（2026-09-26・Claude）: (3) 射幸心を煽る演出。良を重ねるとステージ奥に柴犬が1匹ずつ集まる「おうえん団」（最大20匹、ビートで前足を上げ下げ、満員で「満員御礼！」と花火）、30コンボ以上の「フィーバー」（金色のレーン、バンザイとペンライト、ほねっこ2倍）、完走でもらえる「ほねっこ」、ほねっこで衣装をひく「しばガチャ」（1回/10連、昇格演出つきのカプセル、21種の衣装・毛色、しばずかん）を追加。当たった衣装の柴犬が次の演奏からおうえん団に出る。点数・判定には影響しない。横向きのスマホで結果画面・タイトル画面が収まるよう調整。Unit 149件、Python 41件、E2E 48件（U29・U30を追加、Chromium/WebKit）成功。公開：run 36177816141 は、Ubuntu ChromiumのU29（遅い環境でテストが叩く時刻が遅れ、良にならなかった）とmacOS WebKitのM01（譜面工房の試聴、時々起きるもの）で失敗。U29は叩く時刻を正確に渡す形に直し、M01は失敗時にボタンの状態の移り変わりとお知らせを記録するようにして、run 36178941361 で両ブラウザ・build・deploy 成功。公開サイトを新規のWebKit（iPhone相当）とChromiumで開き、しばガチャ・音ズレ合わせの画面が表示されエラーがないことを確認。実機（iPhone・TD-17）は未確認（DEVICE=NOT_RUN）。

CP24（2026-09-26・Claude）: ユーザーの確認で、iPhoneの振動は動いたが指を離したときにしか来ないと判明。押した瞬間に振動させるのはウェブでは不可能（Safariの仕様）と説明し、ユーザーの判断でタップの振動機能を削除（設定の項目・試しボタンも削除、以前の保存データは読み込める）。E2E U26は「iPhoneのタップが振動部品なしで1回ずつ当たる」確認に置き換え。verify・E2E 48件（Chromium/WebKit）成功。CIのWebKitでガチャの試験U30がタップの速さで一度失敗（テストの叩き方を修正）、次のrunで両ブラウザ・build・deploy成功。公開サイトの設定画面から振動の項目が消えたことを確認。

CP25（2026-09-26・Claude）: ユーザーの報告「途中でズームされて動かなくなる」に対応：演奏中は表示倍率を1に固定（拡大されていても戻る）、Safariのピンチと2本指の動きを取り消し、スマホの選択欄は16pxにして自動拡大を防止。ユーザーの感想で、おうえん団（20匹）をやめて4匹の仲間に戻し、バンドに仲間の4枠（「あと1匹！」）、来たときの名前の札、全員集合（花火・ほねっこ＋30）を追加。ガチャの衣装は4匹が着て、レアな毛色は4匹目として来ることがある。

CP26（2026-09-26・Claude）: ユーザーの提案で、4匹の仲間をスロットに。ゲージ30/50/70/100%で1匹ずつ、その場所でスロットの窓が回って毛色が決まる（ふつう4色＋レア3色、レアは1匹目→4匹目で出やすく、ガチャで当てた毛色は2倍）。同じ毛色がそろうとペア・ダブルペア・3匹そろい・4匹そろい（大当たり）でほねっこ、レア柴にもボーナス、3匹目までにそろいかけると「リーチ！」、はずれると「おしい！」。金色の「もしかして…！？」の予告、ほねっこが骨の形で骨マークへ飛ぶ演出も。確率は20万回のシミュレーションで確認（Unitで範囲を検査）。

CP27（2026-09-26・Claude）: 結果画面にごほうびの演出：ほねっこの数え上げ（小さな音・内訳の札）、ボーナススロット（3リール、リーチ、2つ×1.5・3つ×2・ちゃちゃまる3つ×3）、きょうの初プレイ＋50、太鼓レベル（経験値バー、LEVEL UP!でほねっこ＋50）、惜しい一言（あと1ミスでフルコンボ等）。枠をタップですぐ全部表示。スロットの結果は演奏の終わりに決めて保存。横向き・縦向きのスマホで表示を確認。Unit 153件、E2E 48件（Chromium/WebKit）成功、U29を新しい表示に合わせて更新。CP26の公開run 36215723690 はmacOS WebKitのM01（譜面工房の試聴）で失敗：足した記録で、テストのクリックがページに届いていない（ボタンにクリックが来ない）と判明。クリックが届かなかったときだけ押し直す形にした（確認の中身は同じ）。

CP28（2026-09-26・Claude）: 演奏中の小さなごほうび：点数が数え上がり「+点数」が浮かぶ、50コンボごとの大きな文字（100の倍数は虹色と花火）、クリアライン突破、100コンボ以上のスーパーフィーバー（虹色のレーン、ほねっこ3倍）。verify・E2E 48件（Chromium/WebKit）成功。CP25〜CP28を公開（CP26のrunはM01で失敗→テストを直してCP27のrun 36216550396で公開、CP28はrun 36217116880で両ブラウザ・build・deploy成功）。公開サイトを新規WebKit（iPhone横向き相当）で開き、演奏画面で表示倍率が固定されていること、ページエラーがないことを確認。実機は未確認（DEVICE=NOT_RUN）。

CP29（2026-09-26・Claude）: 気持ちよさの作り込み。叩いた瞬間（判定円が弾む、光の輪、効き線のような光の筋、色つきのかけら、「良」がはんこのように押される、音符がいた場所から残像つきで飛ぶ、パネルの太鼓が弾む、ぴったりの良はさらに明るく）、コンボ（弾み方が強くなる、10コンボごとの輪、切れたら数字が落ちる）、ゲージ先端の光と満タンの光の流れ、連打の吹き出しがふくらみ色が変わる、フィーバー中に舞う光と星（スーパーフィーバーは紙吹雪）、開始の「はじめ！」の破裂、画面の切り替えの扉、結果の点数の「チッチッ」とおみくじの判子。強い点滅・揺れはなし。WebKitで1フレーム約17ms。verify・E2E 48件（Chromium/WebKit）成功、run 36222510556で両ブラウザ・build・deploy成功。

CP30（2026-09-26・Claude）: 選曲・タイトルの細かい演出。選曲画面の上に太鼓レベル・ほねっこ・「きょうの初プレイ ＋50」、タイトルの「はじめる」にも＋50の札。まだ遊んでいない曲に「NEW」、難易度のカードにベストのおみくじの判子。曲を選ぶと札が弾みアイコンが回る、曲名がすべり込みちゃちゃまるが跳ねる、難易度のカードが弾む、開始ボタンがゆっくり光る、指で選んでも「カッ」「ドン」の音。横向きのスマホで開始ボタンまで収まることを確認。verify（Unit 154件）・E2E 48件（Chromium/WebKit）成功。

CP31（2026-09-26・Claude）: 始まりと終わりの盛り上がり。カウントごとの「ピッ」と判定円の脈動・ちゃちゃまるの一打、クリア以上で仲間が跳ねて後光が輝き、フルコンボ・全良では金色の光がレーンを走り花火が5発。ガチャのスーパーレア以上で紙吹雪。CP30の公開は run 36234637045、CP31は run 36235329663 で、いずれも両ブラウザ・build・deploy成功（verify・E2E 48件もローカルで成功）。

CP32（2026-09-26・Claude）: しばガチャ大型アップデート（プレイヤーがコンプリートしたため）。衣装を63種類（N23・R20・SR13・SSR7）に増やし、11のシリーズ（そろえると ほねっこボーナス、全部で＋3000とコンプリート演出）、80回で ウルトラレア確定の天井を追加。しばずかんをシリーズの棚に、スマホでは1列4枚に。ガチャで当てた毛色の柴が演奏中のスロットにも出る。Unit 159件（新規：種類・シリーズの整合、天井、シリーズの支払い、毛色の出現）、E2E U30に天井とシリーズ完成を追加。verify・E2E 48件（Chromium/WebKit）成功、run 36248054203で両ブラウザ・build・deploy成功、公開サイトのガチャ画面で63種類・11シリーズ・天井の表示を確認（新規WebKit、iPhone相当）。

CP33（2026-09-26・Claude）: ユーザーの指摘で「てんしの柴」（犬が亡くなったように見える）を削除し、同じ枠に「獅子舞の柴」（赤い獅子頭が頭をかぷっとかむ縁起もの、唐草の布）を追加。すでに当てていた人は自動で獅子舞の柴に置き換わる（Unitで確認、旧データで演奏画面に出ることも目視）。verify・E2E 48件（Chromium/WebKit）成功、run 36250656044で両ブラウザ・build・deploy成功。公開サイトで「てんしの柴」を持つ旧データを読み込み、しばずかんに獅子舞の柴が入り、てんしの柴が消えていることを確認。
